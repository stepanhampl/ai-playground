services:
  app:
    build: .
    env_file: .env
    volumes:
      - ./workspace:/workspace
    stdin_open: true
    tty: true

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./workspace:/workspace
      - ./backend:/app/backend
